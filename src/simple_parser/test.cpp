#include <stdio.h>

//struct A
//{
//	int b;
//};

int test(int a, int b)
{
//	for(unsigned int i = 0; i < 50; ++i)
//	{
//
//		printf("inside jitted function, heheh %i A.b = %i \n", i, b.b);
//	}
	return (b * (5 + 2)) * (a * (5 + 2));
}
